export default function AdminPage() {
  return (
    <div className="space-y-4">
      <h2 className="text-2xl font-semibold">Admin</h2>
      <p>Zone admin (claims custom requise). Placeholders CRUD Firestore à implémenter.</p>
    </div>
  );
}

