# Local development environment variables
# ADMIN_API_KEY is configured as a Firebase Secret in production

# Base URL for patient app links in emails
# For local testing, use the dev server URL. For production, set this to your real domain.
PATIENT_APP_BASE_URL=http://localhost:3020
